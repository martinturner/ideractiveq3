<%= form_with(model: customer, local: true) do |form| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form class="form-horizontal">
  <fieldset>
    <div class="form-group">
      <label for="inputName" class="col-lg-2 control-label">Customer Name</label>
      <div class="col-lg-10">
          <%= form.text_field :name, id: :customer_name %>
      </div>
    </div>
      <div class="form-group">
      <label for="inputName" class="col-lg-2 control-label">Customer Code</label>
      <div class="col-lg-10">
        <%= form.text_field :customer_code, id: :customer_customer_code %>
      </div>
    </div>
  </fieldset>
  <div class="actions">
    <%= form.submit %>
  </div>
</form>

<% end %>
